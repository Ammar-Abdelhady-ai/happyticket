apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-bucket-tls
  namespace: minio
spec:
  secretName: minio-bucket-tls
  dnsNames:
  - bucket.ticketshappy.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
